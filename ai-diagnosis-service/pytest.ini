[pytest]
testpaths = tests
python_files = test_*.py
# 沙箱环境不允许写 .pytest_cache，关闭缓存插件保持输出干净
addopts = -q -p no:cacheprovider -m "not live"
markers =
    live: 需要真实 DEEPSEEK_API_KEY 的联调测试（默认跳过，用 -m live 显式执行）
